{% extends 'base.html.twig' %}

{% block title %}Browse our bookshelf{% endblock %}

{% block body %}
<section class="content">
    <div class="container-fluid">
        <div class="offset-2 col-auto">
            <div class="row">
                {% for rent in rentList %}
                    <div class="col-3 book_card card">
                        {# <img src="{{ asset('build/images/sale.png') }}" class="card-img-top" alt="SALE IMAGE"> #}
                        <div class="card-body">
                            <h3 class="card-title">{{ rent.getBook.title|u. truncate(25, '...') }}</h3>
                            <h5 class="card-title"> Release : {{ rent.getBook.releaseDate|date('d/m/Y') }} </h5>
                            <p class="card-text">{{ rent.getBook.description|u.truncate(20, '...')}}</p>
                            <p class="card-text">
                                {% for author in rent.getBook.getAuthors() %}
                                    {{ author.firstname }} {{ author.lastname }}</p>
                                {% endfor %}
                            <a href="{{path('app_book_info', {id: rent.getBook.id}) }}" class="btn btn-primary">Voir les détails</a>                 
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
    </div>
</section>
{% endblock %}
